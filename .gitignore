# Ignore OS + editor cruft
.DS_Store
Thumbs.db
.vscode/

# Built assets (we don't have any yet, but future‑proof)
dist/
build/
node_modules/
.env         # make sure it never re-enters Git
.env         # make sure it never re-enters Git
git-filter-repo.py
replacements.txt
src/webpack.config.js
.env         # make sure it never re-enters Git
